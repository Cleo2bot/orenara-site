arity report accepted. The divergence is confirmed as a real defect in quoteCalc — it will be fixed in this task as a new Phase 4, after Phase 3. Sequence:

PROCEED TO PHASE 3 — roll the approved Pathway pattern to Pergola, Pool & Water Feature, and Custom Zone pages per the brief. Worked example copy identical on all four. Pool page: kit.note callout stays where it is AND the licensed-electrician sentence renders once adjacent to the "Start your quote" button. Remove the old CTA block on all three (same primary button + secondary "or enquire for pricing" text link pattern, ?kit=<slug> per page).

ALSO IN THIS PHASE — homepage changes, two parts:
PART 1 — GALLERY MOVE: Gallery currently renders after HowItWorks; move it to immediately after the Environments closing CTA block, before KitComponents. Reorder only. Confirm first-slide priority and lazy-loading unchanged.
PART 2 — PROOF BAND: New small component (ProofBand.tsx) rendered directly above the Enquire section. Three static images side by side (stacked or 1+2 on mobile), equal height: orenara-step-edge-night.webp, orenara-pool-pavilion-tropical.webp (already in /public/images/gallery/, currently unreferenced), orenara-garden-rockbed.webp. No carousel, no captions, no CTA. Small eyebrow label rendering exactly: "THE SYSTEM, AFTER DARK" — flag at checkpoint if it reads better with no label. next/image, lazy, correct sizes. Alt text: step-edge-night — "Step edges lit at night by Orenara LED strip beneath each nosing"; pavilion-tropical — "Tropical pool pavilion at dusk with warm Orenara strip lighting along the pool surround"; garden-rockbed — "Garden rock bed wall washed in warm light from Orenara LED strip at its base".

CHECKPOINT 3 — hard stop: screenshots of all four kit pages' new sections, new homepage section sequence, proof band desktop + mobile, typecheck + production build. Wait for "proceed".

PHASE 4 (only after Checkpoint 3 approval) — ALIGN quoteCalc TO THE REAL PACKING RULE:
1. Replace the ceiling-division driver count with greedy first-fit identical to bom.ts: iterate runs in order, max 12.0m per driver, a run never splits, open a new driver when the current run overflows. Do not import or refactor bom.ts — replicate the logic in quoteCalc and leave the admin engine untouched.
2. Add a connector/end-cap line to QuoteTotals and the BOM panel: one set per run, labelled "IP68 connector + end-cap set", part OR-CON-IP68. Include it in the /enquire payload totals and in PrintQuoteView.
3. Verify dimmer and plug counts follow driver count (1 per driver) after the change.
4. Test cases to report with expected vs actual: [11, 7] → 2 drivers, 2 sets; [7, 7, 7] → 3 drivers, 3 sets; [12] → 1 driver, 1 set; [12.1] → flag/handle per existing >12m behaviour — report what quoteCalc currently does with a single run over 12m before changing anything about that behaviour.

CHECKPOINT 4 — hard stop: test case results, BOM panel screenshot, payload sample, typecheck + production build. No push to GitHub until everything through Checkpoint 4 is approved — one push for the full task.